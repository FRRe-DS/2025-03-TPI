export enum ShippingStatus {
    CREATED = 'created',
    RESERVED = 'reserved',
    IN_TRANSIT = 'in_transit',
    DELIVERED = 'delivered',
    CANCELLED = 'cancelled',
    IN_DISTRIBUTION = 'in_distribution',
    ARRIVED = 'arrived',
    RETURNED = 'returned'
}