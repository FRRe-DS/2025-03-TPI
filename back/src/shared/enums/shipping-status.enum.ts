export enum ShippingStatus {
    PENDING = 'pending',
    CREATED = 'created',
    RESERVED = 'reserved',
    IN_TRANSIT = 'in_transit',
    DELIVERED = 'delivered',
    CANCELLED = 'cancelled',
    RETURNED = 'returned'
}