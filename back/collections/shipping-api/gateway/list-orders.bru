meta {
  name: List Orders via Gateway
  type: http
  seq: 3
}

get {
  url: {{base_url}}/shipping?page=1&items_per_page=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
