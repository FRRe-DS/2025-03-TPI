meta {
  name: Gateway Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/gateway/health
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Gateway is ok", function() {
    expect(res.getBody().status).to.equal("ok");
  });
}
